{

f(int x, var int out);
g(int x, var int out);

f(int x, var int out) {
    if (x < 0) then {
         out = 600 / 2;
    } else {
        g(x - 1, out);
    }
}

g(int x, var int out){
    if (out < 0) then {
        out = 600 / 4;
    } else {
        f(x - 1 , out);
    }
}

int out = 600;

g(out, out);
print out;

f(out, out);
print out;

}
