{
  int ONE = 1;
  printfactorial(int x) {

      fact(var int out, int x) {
            if ( x > 1 ) then {
                fact(out, x - 1);
                out = x * out;
            } else {
                out = ONE;
            }
      }

      int out = 1;
     {{ fact(out, x); }}

      print out;
  }

  // 3628800
  printfactorial(10);
}