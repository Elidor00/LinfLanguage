{
  printfactorial(int x) {

      fact(var int out, int x) {
            if ( x > 1 ) then {
                int old = out;
                fact(out, x - 1);
                out = old * out;
            } else {
                out = 1;
            }
      }

      int out = 1;
      fact(out, x);

      print out;
  }

  // 3628800
  printfactorial(10);
}