{
bool out = false;

isEven(int x, var bool out);
isOdd(int x, var bool out);

isEven(int x, var bool out) {
    if (x == 0) then {
        out = true;
    } else {
        isOdd(n - 1, out);
    }
}

isOdd(int x, var bool out) {
    if (x == 0) then {
        out = false;
    } else {
        isEven(n - 1, out);
    }
}

isEven(100, out);
print out;

isOdd(100, out);
print out;
}