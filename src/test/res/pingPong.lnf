{
int out = -1;

ping(var int out);
pong(var int out);

ping(var int out){
        if (out == 0) then { print 1000;} else {
             if (((out / 2) * 2) == out) then {
                 print 0;
             } else {}
             out = out  - 1;
             pong(out);
        }
}

pong(var int out){
        if (out == 0) then { print 1000;} else {
             if ((((out / 2) * 2) == out) == false) then {
                 print 1;
             } else {}
             out = out  - 1;
             ping(out);
        }
}

ping(out);
print out;

out = 12;
pong(out);
print out;
}
