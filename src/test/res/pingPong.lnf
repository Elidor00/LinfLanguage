{
int out = -1;

ping(var int out);
pong(var int out);

ping(var int out){
        if (out <= 0) then { print 1000;} else {
             out = out  - 1;
             pong(out);
        }
}

pong(var int out){
        if (out <= 0) then { print 1001;} else {
             out = out  - 1;
             ping(out);
        }
}

ping(out);

out = 12;
pong(out);

out = 50;
ping(out);
}
